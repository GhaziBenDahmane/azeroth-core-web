services:
  portal:
    build: .
    image: azeroth-portal:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
